# Derived docker compose for softhsm testing

# To enable the softhsm container for testing, execute
#  docker-compose -f docker-compose.yml -f docker-compose-softhsm.yml
# See the docker-compose documentation for more details

version: "3"
services:
  linotp:
    image: linotp-softhsm
